[secrets]
    # These GitHub package tokens live in the DNAstack account, not the ambient
    # OP_ACCOUNT (iaiteam). Pin reads to this account so they resolve correctly.
    github.account = "team-dnastack.1password.com"
    github.tokens.maven_settings = "op://Employee/Github PAT/credential"
